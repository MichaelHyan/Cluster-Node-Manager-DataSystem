# 集群节点式对话管理系统
<pre>
     ___                                ___
   //   \\ ||\\   || ||\\   ||\\   || ||   \\
  ||       || \\  || || \\  || \\  || ||   ||
  ||       ||  \\ || ||  \\ ||  \\ || ||   ||
   \\___// ||   \\|| ||   \\||   \\|| ||___//
 ======Cluster Node Manager DataSystem=======
</pre>

## 📖 项目简介

CNMD 是一个创新的 Agent 型 AI 对话管理系统，旨在解决当前对话式大模型存在的核心痛点：**对话给出了不符合预期的答案，无法回退，继续对话容易被先前对话记录带偏。**

CNMD 借助类节点会话管理模式，支持对话状态的保存、加载与回退，让用户可以灵活地在不同对话分支间切换。同时，CNMD 赋予了大模型文件读写、终端命令执行、网络爬虫、多模态处理等 Agent 工具能力，使其能够真正完成工作任务而非仅仅对话。

**如果你需要的是一个可以辅助你进行工作，可以按照你的工作节奏走而非完全取代一个人的工作的工具，则你可以选择本项目。本项目虽具有工作流能力，但不建议滥用。**

**任何由任意Agent/workflow框架产生的工作结果均需要你的审查，而不是将工作要求甩给它了事。你需要了解你的具体需求，也需为Agent生产结果负责。**

**如果你需要的是一个可独立进行任务的“员工”，还请使用Harness Agent等主流框架。**

## ✨ 核心特性

- **🔗 节点式对话管理**：支持保存、加载和回退对话状态，像 Git 分支一样管理对话走向
- **⚡ 事件驱动模式**：大模型可自主调用 Agent 工具链（文件操作、终端、网络），循环执行直到完成任务
- **🧰 自定义工具能力**：允许用户自行编写工具链并由Agent调用
- **🧠 记忆数据库系统**：支持对话记忆的智能提取、持久化存储、混合检索与自动整理，让 Agent 拥有长期记忆
- **📁 文件操作能力**：提供目录浏览、文件读写、删除、备份等完整操作
- **💻 终端命令执行**：支持 CMD / PowerShell 命令执行与结果获取
- **🌐 网络访问能力**：内置网页抓取、Ping 检测、自定义 Header 等网络工具
- **🖼️ 多模态支持**：支持图片、音频、视频的原生多模态处理
- **🤖 子任务系统**：支持将复杂任务拆分为多个子任务，由独立子智能体并行执行
- **🎯 技能模块系统**：可扩展的 Skill 模块架构，支持动态加载技能文档
- **💬 多端接入**：支持命令行、Web UI、微信三种交互模式
- **📝 自动日志记录**：每次会话自动创建日志并记录完整对话内容
- **🎭 人设系统**：支持加载不同人设与人格，适应多种对话场景

## 🚀 快速开始

### 1. 安装依赖

如你需要所有运行方式：

```bash
pip install -r requirements.txt
```

依赖包含：
- `openai` — LLM API 调用
- `flask` + `flask-socketio` — Web UI 服务，非必须
- `requests` — HTTP 请求
- `pycryptodome` — 微信文件加解密
- `qrcode` — 终端二维码显示

如你仅使用CLI/微信ClawBot模式，可以不安装flask。

### 2. 配置

在`config.json`中填入你的 API 信息：

```json
{
    "API_KEY": "sk-your-api-key",
    "BASE_URL": "api-base-url",
    "MODEL": "model-name",
    "base_path": "workspace-path",
    "lang":"zh_cn",
    "break": true
}
```

| 配置项 | 说明 |
|--------|------|
| `API_KEY` | OpenAI 兼容 API 密钥 |
| `BASE_URL` | API 基础 URL（支持任何 OpenAI 兼容接口） |
| `MODEL` | 使用的模型名称 |
| `base_path` | Agent 文件操作的根目录，Agent将视该目录为你的项目目录 |
| `lang` | Agent 工具使用的语言，目前支持中文，英文，也可自行在config.json和lang进行配置 |
| `break` | 重复指令处理方式，`true` 为直接中断任务，`false` 为拒绝但继续 |

你可以修改config_model.json对OpenAPI参数进行指定，如修改temperature:

```json
{
    "temperature": 1,
    "其他参数名": "其他参数"
}
```
注：此配置仅对支持该功能的大模型生效，如deepseek-v3.2不会因temperature参数的存在调节输出。

### 3. 启动

根据需求选择启动方式：

| 模式 | 启动命令 | 说明 |
|------|---------|------|
| 命令行模式 | `python CNMD_cli.py` 或 `start_cli.bat` | 终端交互，直接输入命令 |
| Web UI 模式 | `python CNMD_webui.py` 或 `start_webui.bat` | 浏览器访问 `http://localhost:5000` |
| 微信模式 | `python CNMD_wechat.py` 或 `start_wechat.bat` | 微信扫码登录后通过微信对话 |

注意：直接启动cnmd.py为调试模式，虽然具有大部分功能，但不建议使用。

## 📌 核心概念

### 事件驱动模式

不同于传统的大模型对话，事件模式下大模型可选择操作 Agent 工具而不直接回答用户，并等待工具返回结果后继续执行，直到完成任务。从用户下达命令到大模型完成命令之间的过程为一个**事件**。

如仅对话，则一次回复即为一个事件。

### 节点操作

在每次事件开始前，可存档当前对话记录，随时回退到任意节点重新开始：

```text
    #node save eventb
          ↓
eventA-eventB-eventC-eventD->...
(init)    |
          | -> #node load eventb
          |
       eventE->eventF->....
      (->eventB)
```

### 用户命令

用户可直接输入以 `#` 开头的命令控制系统：

| 命令 | 说明 |
|------|------|
| `#node save <名称>` | 保存当前对话状态到指定节点 |
| `#node load <名称>` | 从指定节点加载对话状态 |
| `#node list` | 列出所有已保存的节点 |
| `#node backward <轮数>` | 回退指定轮数的对话（默认1轮） |
| `#mem save` | 提取当前对话的关键信息并保存到记忆数据库 |
| `#mem analyse` | 调用大模型对记忆数据库进行整理、去重和压缩 |
| `#bot reasoning on/off` | 开关思考内容返回 |
| `#bot reset` | 清空对话记录 |
| `#bot prompt <人设名>` | 切换人设 |
| `#backup` | 备份当前工作目录 |
| `#help` | 显示帮助信息 |

## 💬 三种使用模式详解

### CLI模式

最基础的模式，在终端中直接与 CNMD 交互：

```bash
python CNMD_cli.py
```

使用终端直接使用，可用于代码协助。

允许多行输入，输入指令后回车键入空字符(打两次回车)将指令传达至CNMD，可拖拽文件将地址复制到shell

允许使用#pause指令强制暂停工作。

### Web UI 模式

基于 Flask-SocketIO 的实时网页聊天界面，支持局域网多设备访问。

此模式下你需要先输入@bot来调用Agent。

```bash
python CNMD_webui.py
```

- 访问地址：`http://localhost:5000`
- 局域网设备访问：`http://[服务器IP]:5000`
- 使用 `@bot` 前缀或 `#` 命令触发 Agent 处理
- 支持自定义昵称和气泡颜色
- 支持在线用户列表显示

> ⚠️ 如 5000 端口不可用，可尝试 8000/8080/3000 端口。由于使用 HTTP 协议，浏览器可能会显示安全提醒，属于正常现象。

### 微信模式

通过微信 iLink Bot API 接入微信，实现微信内与 Agent 对话：

```bash
python CNMD_wechat.py
```

- **首次启动**：终端显示二维码，使用微信扫码登录
- **凭证保存**：登录成功后凭证自动保存至 `wechat/credentials.json`，后续启动无需重新扫码
- **重新登录**：删除 `wechat/credentials.json` 后重启即可
- **支持消息类型**：文本、图片、语音（自动转文字）、文件、视频
- **文件自动下载**：接收的图片/文件/视频自动下载至 `./files/` 目录
- **文件发送**：Agent 可通过 `send` 指令向用户发送文件
- **定时提醒**：Agent 可创建定时器，到时自动发送提醒消息
- **停止回复**：发送 `#pause` 可强制终止任务

## 🧩 技能模块系统

CNMD 内置可扩展的技能模块系统，技能文档位于 `./skills/` 目录，Agent 可根据需要动态读取并学习：

当前已有技能如下

| 技能 | 文件 | 说明 |
|------|------|------|
| 计时器 | `timer.md` | CNMD 计时器接口规范 |
| 多模态 | `multimodal.md` | 图片/音频/视频处理接口 |
| 文件发送 | `sender.md` | 微信文件发送接口 |
| 技能创建 | `skill_creator.md` | 创建和编辑技能文档 |
| 子任务控制 | `submission.md` | 多子任务分发与执行 |
| 压缩归档 | `ziparchive.md` | 文件压缩/解压缩 |
| 天气查询 | `weather.md` | 天气与预报查询 |
| 人格提取 | `personality_create.md` | 从对话中提取人格特征 |
| 人设加载 | `personality_load.md` | 加载预设人设 |
| 心理评估 | `psych_assessment.md` | 隐式心理状况评估 |

你可以自行编写skill，或者令Agent自行总结并写入。

注意：这不是Memory.md，如你需要将技能记忆存储为skill，确保你保存的是技能方式而不是历史。

## 🧠 系统提示词自定义

允许对系统提示词进行自定义。

### 结构

系统提示词包含以下部分：
- 基础人设/persona：基础设定
- 基础能力/skills：基础agent工具
- 特殊规则/extra：额外规则，根据需要定制

以上均以md skill形式保存在prompt_loader目录下，根据需要配置。

### 配置方式

1.自行将人设文件(persona)，规则文件(extra)分别放置于对应目录，确保为md格式。
1.1人设可自行编辑，或使用Agent的人设提取模块辅助生成。
1.2建议使用默认工具文件。本项目使用自定义工具链，不支持tool calling api。
1.3三个文件均非必要，可在配置文件对应位置填"none"表示不存在该部分。建议保留基础人设。
2.在配置文件(./prompt_loader/config.json)中加入以下信息：

```json
"[你的预设名]":{
        "persona":"[你的人设文件名，不包含后缀]",
        "skills":"skill_base",//使用默认工具，不希望有该能力可以留空或者标记为none
        "extra":"[你的规则文件名，不包含后缀]"
    }
```
3.在程序中将cnm.set_prompt('agent_wechat')语句改为cnm.set_prompt('[你的预设名]')

## 🧰 自定义工具链

你可以为CNMD自定义工具。

1. 工具链程序：任意，确保他可以被调用，参数不能为空，可以使用空参数。
2. 在./tool_handler.py中写入工具调用，注意必须包括以下json返回信息：

```json
{
    "sys":"[如你的Agent需要工具返回的信息，则在这里填上你的工具调用返回信息]",
    "cli":"[返回给用户(你)的调用信息]"
}
```
3. 如果你想要你的自定义工具拥有多语言，则在./lang/目录中的语言键文件中加入自己的自定义键。
4. 告诉Agent如何调用你的工具。你可以将写为skill，或者直接在系统提示词中加入调用方法。

## 🎭 人设系统

CNMD 支持通过 Prompt 加载不同人设。

自定义人设文件可放置于 `./persona/` 目录。

## 📂 日志系统

每次会话自动创建日志文件，保存在 `./logs/` 目录下：

- `{时间戳}.json` — 完整对话记录
- `{时间戳}_node.json` — 节点状态快照

## 🧠 记忆数据库系统 (Memory Database)

CNMD 内置了基于大模型辅助的记忆数据库系统。

不同于传统的Memory.md，CNMD有独立的记忆向量储存区，使用#mem指令调用。

### 核心功能
- 使用总结将重要信息存储为记忆键，持久化保存在 `database/mem.json` 中。
- 调用时大模型可根据关键词自行调用，匹配机制为 Jaccard 相似度，匹配失败则使用大模型二次匹配。
- 整理机制：可使用大模型对重复，复杂记忆键化简。

### 使用指南

#### 用户命令
用户可通过以下命令手动管理记忆库：

| 命令 | 说明 |
|------|------|
| `#mem save` | 总结当前对话上下文，提取关键信息并存入记忆数据库 |
| `#mem analyse` | 对现有记忆数据库进行全局整理、去重和压缩 |

## ⚠️ 注意事项

- 使用前请在 `config.json` 中正确配置 API 信息
- 请将 `base_path` 设置为 Agent 可操作的工作目录
- **重要**：使用 Agent 文件操作前请做好备份，CNMD 内置 `#backup` 命令可供使用
- 微信模式接收的文件自动保存至 `./files/` 目录
- 本项目互联网部分使用 HTTP 协议

## 📄 许可证

本项目采用 Apache 2.0 许可证，详见 [LICENSE](LICENSE) 文件。