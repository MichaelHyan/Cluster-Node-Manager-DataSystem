## CNMD用户指南

出于目前的Agent技术仍处于早起阶段，大众对该类工具的理解尚有不足，特地作此CNMD专用用户指南。

### CNMD是什么？

CNMD全程集群节点式会话管理系统(cluster node manager DataSystem)，为一种支持节点式长会话管理的超轻量级Agent工具，可以完成任务规划，执行等全流程操作，同时具备插件能力，可兼容部分现有技能模块(skill)，网络能力，兼容任何支持使用OpenAI API的大模型。

人话：超轻量级的龙虾，但用轻量化和稳定性换了多平台能力支持。

### 它能做什么？

CNMD自身为支持基础文件和网络技能的Agent，允许对工作区进行文件自主读写操作，拥有基础网络爬虫技能，可通过访问网页源代码了解信息。

技能模块可为CNMD扩展能力，如允许使用多模态能力，人格蒸馏，计时器管理，或者你想加什么就加什么。

### 你应该怎么做？

CNMD使用保守的规划策略。它在执行任务前会规划任务步骤并在得到你的批准下执行。

注意：CNMD不是万能的，其他Agent工具也是如此。你需要向它表达清晰的指令，如：

```bash
根目录下的main.py为主程序，tools目录下为扩展程序，浏览这些程序并列出依赖表，并创建启动脚本。启动脚本应先检查环境是否支持（是否已经安装python3.12并具备所需第三方库），然后启动主程序。
```

如果你要载入模块，只需对它说：

```bash
载入xxx技能模块
```

如果你不放心可以直接教它怎么做，毕竟skill不是说跟软件一样插上就有用的，本质上只是预设版的prompt。

以上所有都在你对你的项目心里有底的情况下操作，如果你不懂那可以让CNMD看一遍再告诉你用法。指令合理并不是因为工具不够稳定，而是做任何事都需要清晰的方式，这一点对那些认为只要AI够智能就无所不能的使用者尤为重要。乱下指令跟把实习生拖到史山代码面前然后让他赶紧进行全栈管理一样荒唐，希望你们理解我在说什么。

### 我怎么管理CNMD的已有资源？

##### 工作目录

就是你在config里填的。

##### 技能模块

在CNMD项目根目录/skills下。

##### 接收文件目录

使用微信发送文件给clawbot时文件会被暂存在CNMD项目根目录/files下，你可以告诉CNMD你刚才给他发了文件。

##### 发送文件

令CNMD载入文件发送技能模块，要啥发啥，直接发到你微信上，注意一次只能发送单文件。

### 为什么要使用CNMD？

没必要。想用什么用什么。

### 如果我要用CNMD应该怎么做？

如果你看到了这个文档说明你至少已经得到了这个项目的主页地址，或者这个项目文件。

你需要选择任意一个你认为靠谱的大模型，找到对应的开发者网站，注册账号，申请API，把API_KEY和文档提供的BASE_URL和模型名填入config.json。

其余部署步骤请看README.md。

### 有什么推荐的大模型

一搜就有，不必纠结。
